version: '3'
services:
  ubuntu-k8s:
    build:
      context: ./
    image: r19750927/kubectl:latest
    container_name: ubuntu-k8s
    hostname: ubuntu-k8s
    volumes:
      - ./config:/root/.kube/config:ro
      - ./hosts:/etc/hosts:ro
      - ./data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
        
